package org.example;

import java.util.Optional;

public class RentalSummaryService {

    private Optional<RentalSummary>
    generateRentalSummary(Contract contract)
    void printRentalSummary(Contract contract)
}
